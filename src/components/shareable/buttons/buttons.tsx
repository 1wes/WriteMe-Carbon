import React, { FunctionComponent, Fragment } from "react";
import Button from "@carbon/react";

const WriteMeButton: FunctionComponent<{
  text: string;
  children: React.ReactNode;
}> = ({ text, children }) => {
  return (
    <Fragment>
      <Button></Button>
    </Fragment>
  );
};
